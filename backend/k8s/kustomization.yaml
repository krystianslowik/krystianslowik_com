apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
# secret.example.yaml is intentionally not listed: create the chat-api-secrets
# secret out of band before applying (see secret.example.yaml).
