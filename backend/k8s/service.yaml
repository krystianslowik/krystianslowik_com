apiVersion: v1
kind: Service
metadata:
  name: chat-api
  labels:
    app: chat-api
spec:
  type: ClusterIP
  selector:
    app: chat-api
  ports:
    - name: http
      port: 80
      targetPort: http
