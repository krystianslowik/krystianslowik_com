# EXAMPLE ONLY - do not commit real values.
# Either copy this file elsewhere, fill it in and apply it manually, or create
# the secret directly:
#   kubectl create secret generic chat-api-secrets \
#     --from-literal=OPENAI_API_KEY=sk-... \
#     --from-literal=DATABASE_URL=mysql://user:password@host:3306/database
apiVersion: v1
kind: Secret
metadata:
  name: chat-api-secrets
type: Opaque
stringData:
  OPENAI_API_KEY: sk-replace-me
  # Optional: remove this key to run without MySQL logging.
  DATABASE_URL: mysql://user:password@mysql.internal:3306/chatlogs
